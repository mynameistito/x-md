# Security Policy

Report vulnerabilities privately through this repository's **Security** tab by opening a private vulnerability report. Do not include API keys, private-account data, or sensitive search terms in public issues.

Only the current `main` branch is supported while the hosted API is beta. The service handles public X identifiers and content only; it does not accept X account credentials. There are currently no tagged or signed releases.
